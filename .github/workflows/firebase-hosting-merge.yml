# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Debug Firebase Service Account Secret
        run: echo "Firebase Service Account Secret (first 10 chars): ${{ secrets.FIREBASE_SERVICE_ACCOUNT_BYTES_WORLD__BUSINESS }}"
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_BYTES_WORLD_BUSINES }}
          channelId: live
          projectId: bytes-world-busines
